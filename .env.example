# DashScope 服务 API 密钥
# 获取方式：在 DashScope 官网注册并获取 API 密钥
DASHSCOPE_API_KEY=your_api_key_here

# 声音服务提供者
# 可选值: dashscope (默认)
# 未来可能支持: other_provider_name
VOICE_PROVIDER=dashscope

# 默认语音合成模型
# 当前支持的模型: cosyvoice-v1
DEFAULT_TTS_MODEL=cosyvoice-v1

# API 版本前缀
API_V1_PREFIX=/api/v1

# 项目名称
PROJECT_NAME=Voice Clone API

# 服务端口 (用于生产环境)
PORT=8000

# 服务主机 (用于生产环境)
HOST=0.0.0.0

# 调试模式 (开发环境设为 True，生产环境设为 False)
DEBUG=False

# 跨域资源共享配置
# 允许的源, 使用逗号分隔, 例如: https://example.com,https://subdomain.example.com
# 星号表示允许所有源: *
CORS_ORIGINS=* 